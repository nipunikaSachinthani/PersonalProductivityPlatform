# Task Prompt — PPP-101

> Read prerequisites first: the project roadmap, AAOS.md, PRINCIPLES.md,
> ARCHITECTURE_ANALYSIS.md, and ENGINEERING_MATURITY.md.

---

## 1. Task Identity

| Field | Value |
|---|---|
| **Task ID** | `PPP-101` |
| **Phase** | Phase 10 — Comprehensive Testing |
| **Module** | Testing |
| **Priority** | P0 |
| **Estimate** | 3h |
| **Dependencies** | PPP-100 |
| **Risk Class** | R1 |

---

## 2. Objective

> Backfill backend integration tests: WebApplicationFactory tests for auth, tasks, projects, notes, calendar, and notifications modules.

Integration tests verify the full pipeline — middleware, routing, validation, handler, database — using WebApplicationFactory with an in-memory or LocalDB test database. Each module gets a dedicated test class covering its key workflows: auth (register→login→get profile→refresh→logout), tasks (CRUD + complete/reopen), projects (CRUD + members + task assignment + authorization), notes (CRUD + search + sanitization), calendar (events + date-range query), and notifications (query + mark read). These tests form the integration layer of the test pyramid and ensure modules work together.

---

## 3. Pre-flight Checklist

Before implementing, confirm:
- [ ] Dependencies are complete (listed above)
- [ ] Read the project roadmap plan file
- [ ] Read AAOS.md (governance boundaries, quality gates, escalation rules)
- [ ] Read PRINCIPLES.md (operational cheat-sheet)
- [ ] Read ARCHITECTURE_ANALYSIS.md for relevant conventions
- [ ] Understand the stack: .NET 8 Clean Architecture, React+Tailwind+shadcn, SQL Server+EF Core
- [ ] If this task requires a decision not covered by docs, stop and ask

---

## 4. Technical Guidance

**Conventions for this module:**
- Test project: `ProductivityPlatform.IntegrationTests`
- Setup: `CustomWebApplicationFactory` that configures test services (in-memory DB, test JWT key, mock blob storage)
- Each module gets a test class: `AuthIntegrationTests`, `TasksIntegrationTests`, `ProjectsIntegrationTests`, `NotesIntegrationTests`, `CalendarIntegrationTests`, `NotificationsIntegrationTests`
- Each test method follows AAA + cleanup (reset DB state between tests or use per-test seeding)
- Test helper: `HttpClient` extensions for auth header, JSON serialization
- Key scenarios per module:
  - Auth: register→login→use token for protected endpoint, duplicate email 409, invalid login 401
  - Tasks: full CRUD cycle, complete→assert status, reopen→assert status, unauthorized access
  - Projects: create with auto-owner-member, add/remove members, task assignment, archive
  - Notifications: complete task→notification created, query notifications, mark read
- Use EF Core InMemory provider or SQLite in-memory for speed

**Concepts practiced in this task:**
- WebApplicationFactory integration testing
- Test data isolation strategies
- Multi-step workflow testing

---

## 5. Acceptance Criteria

- [ ] Objective implemented as stated above
- [ ] Code follows project conventions (naming, folder structure, API design)
- [ ] Validation on all inputs (FluentValidation on backend, Zod on frontend)
- [ ] No secrets in code (use User Secrets / env vars / .gitignore)
- [ ] Tests added: ~15-20 integration tests across 6 modules
- [ ] All existing tests still pass — zero regressions
- [ ] ESLint / dotnet format clean
- [ ] Documentation updated if applicable (README, ARCHITECTURE.md)
- [ ] PR opened with description including what was done and how tested
- [ ] No scope creep — only what this task describes

---

## 6. Out of Scope (explicitly do NOT do these)

- Unrelated refactors outside this task's files
- Performance optimizations not required by ACs
- New third-party dependencies without checking the approved stack
- Features from future phases
- Changing the architecture pattern without discussion

---

## 7. Deliverables

1. **Branch:** `feature/PPP-101-integration-tests` off `main`
2. **Commits:** Conventional format — `test(all): backfill integration tests across all modules`
3. **PR:** Description with what was done, how tested, and a test plan
4. **Tests:** ~15-20 integration tests

---

## 8. Verification Hints

```bash
dotnet test --filter "FullyQualifiedName~IntegrationTests"
```

---

## 9. Escalation Triggers

Stop and ask if:
- A dependency listed above is not complete
- You need a third-party package not in the approved stack
- The task requires a schema change not described here
- You discover a conflict between the roadmap and technical constraints
- You're spending more than 2x the estimate without a clear end in sight

---

## 10. Definition of Done

- All acceptance criteria checked off with evidence
- PR opened and self-reviewed against the checklist
- No TODOs left without a follow-up task reference
